# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/lapangindb
spring.datasource.username=root
spring.datasource.password=nauffalaja
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate (JPA) properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Specify Hibernate naming strategy if needed
# spring.jpa.properties.hibernate.ejb.naming_strategy=org.hibernate.cfg.ImprovedNamingStrategy

# Enable JPA repository scanning (for Spring Data JPA)
spring.data.jpa.repositories.enabled=true

# Enable Spring Boot DevTools (optional for development)
# spring.devtools.restart.enabled=true

# Web server port
server.port=8080
